// === is equal to (checks datatype)
// == does not check datatype
// !== not equal to 
// >= greater or equal to
// <= less or equal to
// > greater then
// < less than
    var n = Math.random();
    n = Math.random(n) * 98;
    n = Math.round(n) + 1 ;
      if (n > 70){
        alert("The chance of relationship between " + x + " and " + y +" is " + n + "%. and you love eachother!!" );
          
          }

          
      else {
        alert("The chance of relationship between " + x + " and " + y +" is " + n + "%.");


          
     }
